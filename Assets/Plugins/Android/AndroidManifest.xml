<?xml version="1.0" encoding="utf-8"?>
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.xbxa01.glassesvr"
    android:versionCode="1"
    android:versionName="0.1.0">

    <!-- min/target SDK (31/34) come from Unity PlayerSettings and are injected into
         build.gradle; declaring <uses-sdk> here conflicts with the merger, so don't. -->

    <!-- Required for SensorManager rotation vector -->
    <uses-feature android:name="android.hardware.sensor.accelerometer" android:required="true" />
    <uses-feature android:name="android.hardware.sensor.gyroscope"     android:required="true" />
    <uses-feature android:name="android.hardware.sensor.compass"       android:required="false" />

    <!-- Screen orientation locked to landscape for 1920×1080 glasses output -->
    <uses-feature android:name="android.hardware.screen.landscape" android:required="true" />

    <!-- Desktop-mode controller needs no extra runtime permissions: the IME's
         BIND_INPUT_METHOD and the accessibility service's BIND_ACCESSIBILITY_SERVICE are
         enforced on the <service> elements and held by the system, and the soft cursor is
         drawn as a TYPE_ACCESSIBILITY_OVERLAY (no SYSTEM_ALERT_WINDOW required). -->

    <!-- Android 11+ package visibility: needed so AppWindow's VirtualAppWindow can
         resolve another app's launch intent (getLaunchIntentForPackage). Without this,
         other packages are invisible and launching them silently fails. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>
    </queries>

    <!-- No <application>-level android attributes: the Unity-generated launcher
         manifest is higher priority and already supplies label/icon/theme from
         PlayerSettings. Declaring them here (a library manifest) only produces
         colliding-attribute merger errors. Window-level settings we DO care about
         (fullscreen theme) go on the activity below, which is ours alone. -->
    <application>

        <activity
            android:name="com.unity3d.player.UnityPlayerActivity"
            android:theme="@android:style/Theme.Black.NoTitleBar.Fullscreen"
            android:screenOrientation="landscape"
            android:launchMode="singleTask"
            android:configChanges="mcc|mnc|locale|touchscreen|keyboard|keyboardHidden|navigation|orientation|screenLayout|uiMode|screenSize|smallestScreenSize|fontScale|layoutDirection|density"
            android:resizeableActivity="true"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Tell Android we handle display changes ourselves (no restart) -->
            <meta-data android:name="unityplayer.ForwardNativeEventsToDalvik" android:value="true" />
        </activity>

        <!-- Desktop-mode pointer/gesture engine. The user enables it once in
             Settings ▸ Accessibility; the system holds BIND_ACCESSIBILITY_SERVICE. -->
        <service
            android:name="com.xbxa01.desktop.XbxAccessibilityService"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/xbx_accessibility_config" />
        </service>

        <!-- Keyboard path. The user enables + selects it once in
             Settings ▸ Languages & input; the system holds BIND_INPUT_METHOD. -->
        <service
            android:name="com.xbxa01.desktop.XbxKeyboardService"
            android:permission="android.permission.BIND_INPUT_METHOD"
            android:exported="true">
            <intent-filter>
                <action android:name="android.view.InputMethod" />
            </intent-filter>
            <meta-data
                android:name="android.view.im"
                android:resource="@xml/xbx_ime_method" />
        </service>

    </application>

</manifest>
