<?xml version="1.0" encoding="utf-8"?>
<!--
    Config for XbxAccessibilityService. We need gesture dispatch and global actions,
    not event snooping, so the event mask is minimal. canPerformGestures="true" is what
    unlocks dispatchGesture(); flagRequestAccessibilityButton is unused but harmless.
-->
<accessibility-service
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:accessibilityEventTypes="typeWindowStateChanged"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:accessibilityFlags="flagDefault|flagRetrieveInteractiveWindows"
    android:canPerformGestures="true"
    android:canRetrieveWindowContent="true"
    android:notificationTimeout="100"
    android:description="@string/xbx_a11y_description" />
