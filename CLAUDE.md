# XBXA01 AR App

## Project
Unity Android AR app for xbxa01 glasses connected to Pixel 9 via USB-C DisplayPort. See `xbxa01/SPEC.md` for full specification.

## Credentials
When this project needs API keys or credentials (e.g., for deployment, signing, or services), use the KeePass automation database as described in the global CLAUDE.md. The automation keyfile allows non-interactive access — no password prompt.
